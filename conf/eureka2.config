/*
 * Example SLURM profile for HPC users.
 * This file contains NO private paths.
 * Users must provide database paths in their own config file
 * or with command-line parameters.
 */

process {
    executor = 'slurm'
    queue    = 'shared'
}

conda {
    enabled = true
}
